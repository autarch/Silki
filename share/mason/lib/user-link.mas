<a href="<% $uri %>">\
% if ( my $image = $user->image() ) {
<img src="<% $image->uri( view => 'mini' ) %>" />\
% }
 <% $user->best_name() %></a>

<%args>
$user
$wiki => undef
</%args>

<%init>
my $uri = $wiki ? $wiki->uri_for_member($user) : $user->uri();
</%init>
