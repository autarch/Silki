<table class="standard-table">
  <thead>
    <tr>
      <th><% loc('Member') %></th>
      <th><% loc('Role') %></th>
    </tr>
  </thead>
% while ( my ( $user, $role ) = $users->next() ) {
  <tbody>
    <tr class="<% $users->index() % 2 ? 'odd' : 'even' %>">
      <td><a href="<% $wiki->uri_for_member($user) %>"><% $user->best_name() %></a></td>
      <td><% loc( $role->name() ) %></td>
    </tr>
  </tbody>
% }

<& /lib/table/pager.mas,
   index     => $users->index(),
   pager     => $pager,
   uri_maker => sub { $wiki->uri( view => 'users', @_ ) } &>

</table>

<%args>
$wiki
$users
$pager
</%args>

<%method title>
<% loc( '%1 Members', $wiki->title() ) %>
<%args>
$wiki
</%args>
</%method>
