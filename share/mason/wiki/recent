<table class="standard-table">
  <thead>
    <tr>
      <th>Title</th>
      <th>Date</th>
      <th>Who</th>
      <th></th>
    </tr>
  </thead>
% while ( my ( $page, $revision ) = $pages->next() ) {
  <tbody>
    <tr class="<% $pages->index() % 2 ? 'odd' : 'even' %>">
      <td><a href="<% $page->uri() %>"><% $page->title() %></a> (revision <% $revision->revision_number() %>)</td>
      <td><% $c->user()->format_datetime( $revision->creation_datetime() ) %></td>
      <td><a href="<% $revision->user()->uri() %>"><% $revision->user()->best_name() %></a></td>
      <td>
%   if ( $revision->revision_number() > 1 ) {
        <a href="<% $page->uri( view => 'diff', query => { revisions => [ $revision->revision_number() - 1, $revision->revision_number() ] } ) %>"><% loc('see changes') %></a>
%   }
      </td>
    </tr>
  </tbody>
% }
</table>

<%args>
$pages
</%args>

<%method title>
<% loc( 'Recent Changes in [_1]', $wiki->title() ) %>
<%args>
$wiki
</%args>
</%method>
